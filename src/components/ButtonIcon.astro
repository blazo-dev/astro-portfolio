---
type Props = {
	href?: string
	target?: string
	isButton?: boolean
	text?: string
}

const { href, isButton, target, text } = Astro.props
---

{
	isButton ? (
		<button class='button'>
			<slot name='icon-before' />
			<span class='button-text'>
				<slot />
				<slot name='icon-in' />
			</span>
			<slot name='icon-after' />
		</button>
	) : (
		<a class='button' href={href} target={target}>
			<slot name='icon-before' />
			{text && <span class='button-text'>{text}</span>}
			<slot name='icon-after' />
		</a>
	)
}

<style lang='scss'>
	.button {
		display: flex;
		align-items: center;
		justify-content: center;
		gap: 0.3em;

		min-width: 38px;
		padding: 0.5rem;

		border: transparent;
		border-radius: var(--border-radius);
		background-color: var(--p-color);
		color: var(--btn-text-color);

		text-decoration: none;

		cursor: pointer;
		transition: var(--color-transition);

		&:hover {
			background-color: var(--s-color);
		}

		&-text {
			display: none;
			font-weight: 700;
			letter-spacing: 0.1em;

			@media (min-width: 768px) {
				display: block;
			}
		}
	}
</style>
